@{
  Layout = "_Layout";
}

@model Factory.Models.Machine

<div class="well">
  <h2>Add a new Machine</h2>
  @using (Html.BeginForm())
  {
      @Html.LabelFor(model => model.Name, new { Style = "color:White" })<span class="labelBoxSpace"></span>
      @Html.TextBoxFor(model => model.Name)<span class="boxToNextSpace"></span>
      <input type="submit" value="Add new machine" class="btn btn-primary" />
  }
  <br>
  <button class="btn btn-warning btn-md" onclick="location.href='@Url.Action("Index")'">Show all machines</button>
</div>